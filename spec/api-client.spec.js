import { predict } from '../src/api-client';

describe('Remote api client', async () => {
  it('should not allow an empty file to be passed as an argument', () => {
    expect(false).toBeTrue();
  });

  it('should allow a jpeg file to be predicted', () => {
    expect(false).toBeTrue();
  });

  it('should allow a png file to be predicted', () => {
    expect(false).toBeTrue();
  });

  it('should not allow other file types to be predicted', () => {
    expect(false).toBeTrue();
  });

  it('should be able to handle relative paths', () => {
    expect(false).toBeTrue();
  });

  it('should be able to handle full paths', () => {
    expect(false).toBeTrue();
  });
});
